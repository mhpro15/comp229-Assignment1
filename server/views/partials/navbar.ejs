<div id="navbar">
  <ul>
    <!-- Logo -->
    <li href="/home"><span class="logo">hiro</span></li>

    <!-- Home link -->
    <% if (title == "Home"){ %>
      <li class="nav-item active">
    <% } else  { %>
      <li class="nav-item">
    <% } %>
      <a class="nav-link" href="/home">Home</a>
    </li>

    <!-- About link -->
    <% if (title == "About") { %>
      <li class="nav-item active">
    <% } else  { %>
      <li class="nav-item">
    <% } %>
      <a class="nav-link" href="/about">About</a>
    </li>

    <!-- Services link -->
    <% if (title == "Services") { %>
      <li class="nav-item active">
    <% } else  { %>
      <li class="nav-item">
    <% } %>
      <a class="nav-link" href="/services">Services</a>
    </li>

    <!-- Projects link -->
    <% if (title == "Projects") { %>
      <li class="nav-item active">
    <% } else  { %>
      <li class="nav-item">
    <% } %>
      <a class="nav-link" href="/projects">Projects</a>
    </li>

    <!-- Login/Contact-List link -->
    <% if (displayName == '') { %>
      <% if (title == "Login") { %>
        <li class="nav-item loginNav active">
      <% } else  { %>
        <li class="nav-item loginNav ">
      <% } %>
        <a class="nav-link" href="/login">Login</a>
      </li>
    <% }else{ %>
       <% if (title == "Business Contacts") { %>
        <li class="nav-item dropdown active"><a>Hi, <%=displayName %>!</a>
    
      <% } else  { %>
       <li class="nav-item dropdown"><a>Hi, <%=displayName %>!</a>
    
     <% } %>
    <div class="dropdown-content">
          <a href="/contact-list">Contact List</a>
          <a href="/logout">Log Out</a>
      </div>
      </li>
      <% }%>
    <!-- Contact Me link -->
    <% if (title == "Contact Me") { %>
      <li class="nav-item active contact">
    <% } else  { %>
      <li class="nav-item contact">
    <% } %>
      <a class="nav-link" href="/contact">Contact Me</a>
    </li>
  </ul>
</div>
